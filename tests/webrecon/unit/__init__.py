"""Example-based unit tests for `webrecon` modules.

Each module under `webrecon.<subpackage>` should have a sibling test
file here named `test_<module>.py`. Unit tests must mock every external
side effect (HTTP, filesystem outside `tmp_path`, subprocesses) so the
suite runs fast and offline (Requirement 11.1, 11.2).
"""
