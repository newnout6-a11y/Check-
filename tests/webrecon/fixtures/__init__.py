"""Shared fixtures and sample payloads for `webrecon` tests.

This package intentionally contains no test modules: it groups
fixture-only helpers and on-disk corpora (sample HTML pages, FOFA /
Shodan / Serper / GitHub API response bodies) used across the unit,
integration and property suites.
"""
