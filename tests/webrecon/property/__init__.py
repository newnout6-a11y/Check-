"""Property-based tests for `webrecon` (Hypothesis).

Custom strategies live in ``tests/webrecon/strategies.py``. Each
property test file documents the property under test in a module-level
docstring and links back to the matching numbered acceptance criterion
in ``.kiro/specs/web-reconnaissance/requirements.md`` via a
``Validates: Requirements X.Y`` line per the spec convention
(Requirement 11.3).
"""
