"""Local fixtures for `webrecon` property-based tests.

Hypothesis strategies live in ``tests/webrecon/strategies.py`` so they
can be imported from any test module. Add fixtures here only when a
strategy needs to pull state from a pytest fixture (e.g. a temporary
directory).
"""
