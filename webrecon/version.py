"""Single source of truth for the webrecon package version.

Kept separate from `__init__.py` so build tooling and runtime code can
read it without triggering package imports.
"""

__version__ = "0.1.0"
